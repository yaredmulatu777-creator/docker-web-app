name: Docker CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_YAREDYDM }}
        password: ${{ secrets.DOCKER_dckr_pat_vAO26LKLxE8ak4oMVwfMkV154oU }}
<<<<<<< HEAD
=======

    - name: Build Docker image
      run: docker build -t ${{ secrets.DOCKER_YAREDYDM  }}/docker-web-app:latest .

    - name: Push Docker image
      run: docker push ${{ secrets.DOCKER_YAREDYDM  }}/docker-web-app:latest
>>>>>>> f4717bd (Upgrade CI to build and Push Docker image)

    - name: Build Docker image
      run: docker build -t ${{ secrets.DOCKER_YAREDYDM  }}/docker-web-app:latest .

    - name: Push Docker image
      run: docker push ${{ secrets.DOCKER_YAREDYDM  }}/docker-web-app:latest
